@model IEnumerable<ERPSzakdolgozat.Models.SkillLevel>

@{
	ViewData["Title"] = "Skill Levels - Index";
}

<h2>Skill Levels - Index</h2>

<div class="col-md-12 margin-bottom-sm">
	<a asp-action="Create" class="btn btn-success pull-right">Create New</a>
</div>

<table class="table table-hover">
	<thead>
		<tr>
			<th>
				@Html.DisplayNameFor(model => model.SkillLevelName)
			</th>
			<th>
				@Html.DisplayNameFor(model => model.IsSelectable)
			</th>
			<th></th>
		</tr>
	</thead>
	<tbody>
		@foreach (var item in Model)
		{
			<tr>
				<td>
					@Html.DisplayFor(modelItem => item.SkillLevelName)
				</td>
				<td>
					@Html.DisplayFor(modelItem => item.IsSelectable)
				</td>
				<td class="text-right">
					<div class="dropdown">
						<button class="btn btn-primary btn-sm dropdown-toggle" type="button" data-toggle="dropdown">
							Actions
						</button>
						<ul class="dropdown-menu dropdown-menu">
							<li><a asp-action="Edit" asp-route-id="@item.Id">Edit</a></li>
							<li class="divider"></li>
							<li><a asp-action="Delete" asp-route-id="@item.Id">Delete</a></li>
						</ul>
					</div>
				</td>
			</tr>
		}
	</tbody>
</table>
